#include<stdio.h>
int main()
{
   int a,b,c;
   printf("enter the values of a and b");
   scanf("%d%d",&a,&b);
   switch(c)
   {
   case 1:printf("the sum of two numbers c=%d",a+b);
   break;
   case 2:printf("the sub of two numbers c=%d",a-b);
   break;
   case 3:printf("the multi of two numbers c=%d",a*b);
   break;
   case 4:printf("the division of two numbers c=%d",a/b);
   }
}
